
@no_mangle
int printf(char* fmt);
	u64 fib(u64 n) {
		/*
		u64 a = 0;
		u64 b = 1;
		u64 c = 0;
		for (u64 i = 1; i < n; i = i + 1u64) {
			c = a + b;
			a = b;
			b = c;
			printf("loop!\n\0");
		}
		return c;*/
		return 0;
	}


int main() {
	f64 a = 23.0;
	printf("Hello world!\n\0");
	return fib(32) as i32;
}
