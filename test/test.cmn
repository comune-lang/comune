
@no_mangle
int printf(char* fmt);

	
	u64 fib(u64 n) {
		u64 a = 0;
		u64 b = 1;
		u64 c = 0;
		for (u64 i = 1; i < n; i = i + 1 as u64) {
			c = a + b;
			a = b;
			b = c;
			printf("loop!\n\0");
		}
		return c;
	}


int main() {
	printf("Hello world!\n\0");
	return fib(32) as i32; // Throws internal compiler error rn, int coercion is allowed by semantic phase but no cast is created
}
