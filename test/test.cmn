
@no_mangle
int printf(char* fmt);

namespace cool {
	
	u64 fib(u64 n) {
		u64 a = 0 as u64;
		u64 b = 1 as u64;
		u64 c = 0 as u64;
		for (u64 i = 1 as u64; i < n; i = i + 1 as u64) {
			c = a + b;
			a = b;
			b = c;
		}
		return c;
	}
}

int main() {
	printf("Hello world!\n\0");
	return cool::fib(32 as u64) as i32;
}
