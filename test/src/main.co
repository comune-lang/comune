@no_mangle
void printf(u8* fmt);

i32 main() {
	//mut u64[3] store = [ 0, 1, 0 ];
	mut u64 a = 0;
	mut u64 b = 1;
	mut u64 c = 0;

	for (mut u64 i = 1; i < 10; i++) {
		//store[2] = store[0] + store[1];
		//store[0] = store[1];
		//store[1] = store[2];
		c = a + b;
		a = b;
		b = c;
		printf(c"Loop!\n");
	};

	return c as i32;
	//return store[2];
}