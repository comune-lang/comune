import libc;
import module_test;

namespace cool {
	u64 fib(u64 n) {
		u64 a = 0;
		u64 b = 1;
		u64 c = 0;
		for (u64 i = 1; i < n; i += 1u64) {
			c = a + b;
			a = b;
			b = c;
			libc::printf("loop!\n");
		}
		return c;
	}
}

int main() {
	return cool::fib(10) as i32 + module_test::foo();
}