import module_test;

using module_test::fib;
using module_test::printf;

@layout(packed)
struct Test {
	int a;
	int b;
}

impl Test {
	int sum(ref Test self) {
		printf("This is a method call!\n");
		return self.a + self.b;
	}

	Test new() {
		Test result;
		result.a = 60;
		result.b = 9;
		return result;
	}
}

int main() {
	Test test = Test::new();
	return test.sum();
}
