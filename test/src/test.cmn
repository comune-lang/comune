import module_test;
using module_test::foo;

@no_mangle
int printf(char* fmt);

namespace cool {
	u64 fib(u64 n) {
		u64[3] array_test;
		u64 a = 0;
		u64 b = 1;
		u64 c = 0;
		for (u64 i = 1; i < n; i += 1u64) {
			c = a + b;
			a = b;
			b = c;
			printf("loop!\n");
		}
		return c;
	}
}

int main() {
	float a = 10.f;
	return cool::fib(10) as i32 + foo();
}
