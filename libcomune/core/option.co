
@no_mangle
never abort();

enum Option<type T> {
	Some { T value; },
	None
}

impl<type T> Option<T> {
	T unwrap(self) {
		match self {
			Option<T>::Some { value } => {
				return value
			}

			Option<T>::None _ => {
				abort()
			}
		}
	}
}